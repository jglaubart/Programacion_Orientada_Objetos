Se desea modelar MatchCollection que representa una colección que acepta repetidos donde cada uno de los elementos está asociado a una posición y que cuenta con dos métodos getAllMatched() y getAllMatchedReversed() que retornan otra colección con orden donde se incluyen únicamente los elementos sin repetir de la MatchCollection que cumplan todas las condiciones que se agregaron con el método addMatchPredicate(...).