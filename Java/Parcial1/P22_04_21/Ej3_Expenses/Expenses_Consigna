Se desea modelar el cálculo de expensas para un edificio.
Este sistema debe permitir crear unidades funcionales (departamentos residenciales o locales comerciales)
para luego poder obtener el importe de las expensas para cada unidad funcional.

Todas las unidades funcionales cuentan con un nombre y una cantidad entera positiva de metros cuadrados (no es necesario validarlo).
Las unidades funcionales residenciales cuentan además con una cantidad entera positiva de ambientes (no es necesario validarlo).

El cálculo total de las expensas se basa en una serie de reglas:
Costo por metro cuadrado de la unidad funcional tanto para residenciales como comerciales.
Multiplicador sobre el total de las expensas exclusivamente para las unidades funcionales comerciales.

Cargo extra por ambiente sobre el total de las expensas exclusivamente para las unidades funcionales residenciales.
Ya cuenta con la implementación de la clase Building (se puede modificar) que contiene estos tres valores necesarios para el cálculo
(valores por defecto) y los métodos correspondientes para consultarlos y actualizarlos